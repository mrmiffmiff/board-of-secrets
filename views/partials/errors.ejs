<% if (locals.errors) {%>
    <div role="alert">
        <ul>
            <% for (const error of errors) { %>
                <li><%= error.msg %></li>
            <% } %>
        </ul>
    </div>
<% } %>

<!-- Also just an example really -->