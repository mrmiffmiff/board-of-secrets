<%- include('partials/head') %>
    <main>
        <h1>Welcome <% if(locals.currentUser) { %><%= locals.currentUser.firstName %><% } else { %>Guest<% } %></h1>
    </main>
<%- include('partials/foot') %>